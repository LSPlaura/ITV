<Page x:Class="ITV.Views.BackUp.BackUps"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      mc:Ignorable="d"
      Title="BackUps"
      Background="#FBFBFB"
      TextOptions.TextFormattingMode="Display"
      TextOptions.TextRenderingMode="ClearType"
      UseLayoutRounding="True">

    <Page.Resources>
        <Style x:Key="ActionButton" TargetType="Button">
            <Setter Property="Height" Value="40"/> <Setter Property="Padding" Value="30,0"/> <Setter Property="Margin" Value="12,0,0,0"/> <Setter Property="MinWidth" Value="125"/> <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontSize" Value="14"/>
    <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border" Background="{TemplateBinding Background}" CornerRadius="8">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Opacity" Value="0.85"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="border" Property="ScaleTransform.ScaleX" Value="0.98"/> </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Page.Resources>

    <Grid Margin="25">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/> <RowDefinition Height="*"/>    </Grid.RowDefinitions>

        <DockPanel Grid.Row="0" Margin="0,0,0,20">
            <StackPanel DockPanel.Dock="Left">
                <TextBlock Text="Backups" FontSize="28" FontWeight="Bold" Foreground="#111111"/>
                <TextBlock Text="Gestione sus puntos de restauración" FontSize="13" Foreground="#666666"/>
            </StackPanel>

            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" VerticalAlignment="Center">
                <Button Content="✚ Crear Nuevo" 
                        Style="{StaticResource ActionButton}" 
                        Background="#0078D4"/>
                
                <Button Content="↺ Restaurar" 
                        Style="{StaticResource ActionButton}" 
                        Background="#107C10"/>
                
                <Button Content="🗑 Borrar" 
                        Style="{StaticResource ActionButton}" 
                        Background="#D11A2A"/>
            </StackPanel>
        </DockPanel>

        <ListView Grid.Row="1"
                  ItemsSource="{Binding RutasImportacion}"
                  SelectedItem="{Binding RutaSeleccionada}"
                  HorizontalContentAlignment="Stretch"
                  BorderThickness="0"
                  Background="Transparent"
                  ScrollViewer.HorizontalScrollBarVisibility="Disabled">

            <ListView.ItemContainerStyle>
                <Style TargetType="ListViewItem">
                    <Setter Property="Template">
                        <Setter.Value>
                            <ControlTemplate TargetType="ListViewItem">
                                <ContentPresenter />
                            </ControlTemplate>
                        </Setter.Value>
                    </Setter>
                </Style>
            </ListView.ItemContainerStyle>

            <ListView.ItemTemplate>
                <DataTemplate>
                    <Border x:Name="ItemBorder" 
                            Padding="16" 
                            Margin="0,0,0,12" 
                            Background="White" 
                            BorderBrush="#E0E0E0" 
                            BorderThickness="1"
                            CornerRadius="10">
                        <Border.Effect>
                            <DropShadowEffect BlurRadius="10" ShadowDepth="1" Opacity="0.03"/>
                        </Border.Effect>
                        
                        <DockPanel>
                            <TextBlock Text="📄" Margin="0,0,15,0" VerticalAlignment="Center" FontSize="18"/>
                            
                            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" VerticalAlignment="Center">
                                <Button Content="✚ Crear Nuevo" 
                                        Style="{StaticResource ActionButton}" 
                                        Background="#0078D4"/>
    
                                <Button Content="↺ Restaurar" 
                                        Style="{StaticResource ActionButton}" 
                                        Background="#107C10"/>
    
                                <Button Content="🗑 Borrar" 
                                        Style="{StaticResource ActionButton}" 
                                        Background="#D11A2A"/>
                            </StackPanel>
                        </DockPanel>
                    </Border>
                    <DataTemplate.Triggers>
                        <DataTrigger Binding="{Binding RelativeSource={RelativeSource Mode=FindAncestor, AncestorType={x:Type ListViewItem}}, Path=IsSelected}" Value="True">
                            <Setter TargetName="ItemBorder" Property="BorderBrush" Value="#0078D4"/>
                            <Setter TargetName="ItemBorder" Property="BorderThickness" Value="2"/>
                            <Setter TargetName="ItemBorder" Property="Background" Value="#F0F7FF"/>
                        </DataTrigger>
                    </DataTemplate.Triggers>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
    </Grid>
</Page>