<Window x:Class="ITV.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:ITV"
        mc:Ignorable="d"
        Title="ITV Management System"
        Height="800" 
        Width="1400"
        WindowStartupLocation="CenterScreen"
        TextOptions.TextFormattingMode="Display"
        TextOptions.TextRenderingMode="ClearType"
        UseLayoutRounding="True">

    <Window.Resources>
        <Style x:Key="MenuButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Foreground" Value="#E0E5E5"/>
            <Setter Property="Height" Value="50"/>
            <Setter Property="Margin" Value="12,4,12,4"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="FontWeight" Value="Medium"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="HorizontalContentAlignment" Value="Left"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border" 
                                Background="{TemplateBinding Background}" 
                                CornerRadius="10" 
                                Padding="15,0">
                            <ContentPresenter HorizontalAlignment="Left" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Background" Value="#2AFFFFFF"/>
                                <Setter Property="Foreground" Value="#36FF6E"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="border" Property="Background" Value="#FF63C2"/>
                                <Setter Property="Foreground" Value="White"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <Grid Background="#F0F2F2">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="280"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <Border Grid.Column="0" Background="#7D8A8A">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/> <RowDefinition Height="*"/>    <RowDefinition Height="Auto"/> </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Margin="0,50,0,40">
            <TextBlock Text="ITV SYSTEM" Foreground="White" FontSize="24" FontWeight="ExtraBold" HorizontalAlignment="Center"/>
            <Border Height="3" Width="50" Background="#FF63C2" Margin="0,12,0,0" CornerRadius="1.5">
                <Border.Effect>
                    <DropShadowEffect BlurRadius="10" ShadowDepth="0" Color="#FF63C2" Opacity="0.6"/>
                </Border.Effect>
            </Border>
        </StackPanel>

        <StackPanel Grid.Row="1">
            <TextBlock Text="MAIN NAVIGATION" Foreground="#3A4242" FontSize="10" FontWeight="Black" Margin="28,0,0,12" Opacity="0.7"/>
            <Button Content="📅  Gestión de Citas" Style="{StaticResource MenuButtonStyle}" Click="Citas_Click"/>
            
            <TextBlock Text="ADMINISTRATION" Foreground="#3A4242" FontSize="10" FontWeight="Black" Margin="28,25,0,12" Opacity="0.7"/>
            <Button Content="💾  Backups" Style="{StaticResource MenuButtonStyle}" Click="Importar_Click"/>
            <Button Content="🔄  Export / Import" Style="{StaticResource MenuButtonStyle}" Click="Exportar_Click"/>
        </StackPanel>

        <StackPanel Grid.Row="2" Margin="0,0,0,25">
            <Separator Margin="25,0,25,10" Background="#3A4242" Opacity="0.3"/>
            
            <Button Content="ℹ️  Acerca de" 
                    Style="{StaticResource MenuButtonStyle}"
                    Click="Acercade_Click"/>

            <Border Background="#2A3333" CornerRadius="15" Padding="12,4" HorizontalAlignment="Center" Margin="0,10,0,0">
                 <TextBlock Text="v 1.0.4 - 2026" 
                           Foreground="#36FF6E" 
                           FontSize="11" 
                           FontWeight="Bold"/>
            </Border>
        </StackPanel>
    </Grid>
</Border>

        <Grid Grid.Column="1">
            <Frame x:Name="MainFrame" 
                   NavigationUIVisibility="Hidden"
                   Background="Transparent"
                   HorizontalContentAlignment="Stretch"
                   VerticalContentAlignment="Stretch"/>
        </Grid>
    </Grid>
</Window>