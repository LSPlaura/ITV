<Window x:Class="ITV.Views.ExportCitas.ExportCitas"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="ExportCitas" 
        Height="350" Width="500"
        WindowStartupLocation="CenterScreen"
        ResizeMode="NoResize"
        Background="#FBFBFB">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- HEADER -->
        <Border Grid.Row="0" Background="#7D8A8A" Height="100">
            <StackPanel VerticalAlignment="Center">
                <TextBlock Text="EXPORTAR CITA" 
                           Foreground="White" 
                           FontSize="28" 
                           FontWeight="ExtraBold" 
                           HorizontalAlignment="Center"/>
                <Border Height="3" Width="40" Background="#FF63C2" Margin="0,8,0,0" CornerRadius="1.5" HorizontalAlignment="Center">
                    <Border.Effect>
                        <DropShadowEffect BlurRadius="10" ShadowDepth="0" Color="#FF63C2" Opacity="0.8"/>
                    </Border.Effect>
                </Border>
            </StackPanel>
        </Border>

        <!-- CONTENIDO + BOTONES -->
        <StackPanel Grid.Row="1" Margin="30,20,30,30" VerticalAlignment="Center" HorizontalAlignment="Center">
            <TextBlock Text="Selecciona el formato de exportación" 
                       FontSize="22" FontWeight="SemiBold" 
                       Foreground="#3A4242" HorizontalAlignment="Center" Margin="0,0,0,30"/>

            <!-- BOTONES -->
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                <Button Content="📄 EXPORTAR A HTML" 
                        Width="160" Height="50"
                        Background="#36FF6E" 
                        Foreground="#1A222B"
                        FontWeight="Bold" 
                        FontSize="12"
                        Cursor="Hand"
                        Margin="0,0,20,0">
                    <Button.Style>
                        <Style TargetType="Button">
                            <Setter Property="Template">
                                <Setter.Value>
                                    <ControlTemplate TargetType="Button">
                                        <Border Background="{TemplateBinding Background}" CornerRadius="10">
                                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                        </Border>
                                    </ControlTemplate>
                                </Setter.Value>
                            </Setter>
                            <Style.Triggers>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter Property="Opacity" Value="0.85"/>
                                </Trigger>
                            </Style.Triggers>
                        </Style>
                    </Button.Style>
                </Button>

                <Button Content="📕 EXPORTAR A PDF" 
                        Width="160" Height="50"
                        Background="#FF63C2" 
                        Foreground="White"
                        FontWeight="Bold" 
                        FontSize="12"
                        Cursor="Hand">
                    <Button.Style>
                        <Style TargetType="Button">
                            <Setter Property="Template">
                                <Setter.Value>
                                    <ControlTemplate TargetType="Button">
                                        <Border Background="{TemplateBinding Background}" CornerRadius="10">
                                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                        </Border>
                                    </ControlTemplate>
                                </Setter.Value>
                            </Setter>
                            <Style.Triggers>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter Property="Opacity" Value="0.85"/>
                                </Trigger>
                            </Style.Triggers>
                        </Style>
                    </Button.Style>
                </Button>
            </StackPanel>
        </StackPanel>
    </Grid>
</Window>